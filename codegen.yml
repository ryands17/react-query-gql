overwrite: true
schema:
  - 'https://countries.trevorblades.com/'
documents:
  - src/query.graphql
generates:
  ./src/generated/queries.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
hooks:
  afterAllFileWrite:
    - prettier --write
